import { Card } from '@/components/UI';
import { FC } from 'react';

const Home: FC = () => {
  return (
    <div>
      <h4>Home</h4>
      <Card title="test" />
    </div>
  );
};

export default Home;
